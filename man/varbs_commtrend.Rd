% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{varbs_commtrend}
\alias{varbs_commtrend}
\title{Generate regime-specific VAR parameters with (optional) common stochastic trends}
\usage{
varbs_commtrend(m, p, T, r, Ncommtrend, n, S, r_npo)
}
\arguments{
\item{m}{Number of variables per country.}

\item{p}{Lags (country- and regime-specific). Used as indexed in the function.}

\item{T}{Number of observations.}

\item{r}{Number of stochastic trends per country.}

\item{Ncommtrend}{Number of common trends (optional). If missing, treated as \code{NA}.}

\item{n}{Number of countries in the GVAR model.}

\item{S}{Number of regimes.}

\item{r_npo}{A matrix/array specifying the roots of the characteristic polynomial in lags.}
}
\value{
A list containing:
\itemize{
\item Bo: VAR coefficient array of dimension (m, m, Pmax, n, S)
\item alpha: list of regime-specific adjustment matrices per country
\item beta: list of regime-specific cointegration matrices per country
}
}
\description{
Simulates/constructs VAR coefficient arrays for a multi-country, multi-regime setup
where each country can have a specified number of stochastic trends, and an optional
subset of those trends can be imposed as \emph{common} across countries. The function
returns the regime-specific domestic VAR parameters (\code{Bo}) along with the implied
error-correction representations (\code{alpha}, \code{beta}) obtained via \code{b2_cib()}.
}
\details{
Internally, the function:
\itemize{
\item builds a “unit-root/trend” VAR block for the stochastic-trend subspace per country,
\item optionally replaces the first \code{Ncommtrend} trend block with a shared (common) trend
process across all countries, and
\item builds a stable VAR block for the remaining subspace using provided characteristic
roots (\code{r_npo}), then combines blocks into full (m x m) coefficient matrices.
}
}
\examples{
# Minimal illustrative example (fabricated shapes; relies on var_data() and b2_cib())
# This example shows how to call the function, assuming the helper functions exist.
\dontrun{
set.seed(1)
m <- 4
n <- 2
S <- 2
T <- 100
r <- c(1, 1)
p <- array(2, dim = c(n, 2, S))

# r_npo must satisfy var_data()'s “roots specification” requirements.
# See vignette for recommended construction.
r_npo <- array(0.5, dim = c(m, max(p[, 1, ]), n, S))

out <- varbs_commtrend(m = m, p = p, T = T, r = r, Ncommtrend = 1, n = n, S = S, r_npo = r_npo)
str(out)
}

}
\keyword{internal}
