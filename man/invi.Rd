% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{invi}
\alias{invi}
\title{Compute a masked inverse covariance matrix for selected variables}
\usage{
invi(sigma, c, i)
}
\arguments{
\item{sigma}{The input covariance matrix (n x n).}

\item{c}{The weighting vector of a concerted policy action. Positive entries indicate
selected variables.}

\item{i}{The index of responding variables (integer in 1..n).}
}
\value{
An (n x n) matrix containing the inverse of the selected, masked covariance
submatrix in the corresponding positions, and zeros elsewhere.
}
\description{
Forms an inverse covariance matrix for a selected subset of variables defined by
the weighting vector \code{c}, while zeroing out the covariance contributions of a
designated responding variable index \code{i} (except for its own variance term).
The inverse is computed only on the selected submatrix and then placed back into
a zero-filled (n x n) matrix.
}
\examples{
set.seed(1)
n <- 4
sigma <- crossprod(matrix(rnorm(80), nrow = 20)) / 20
c <- c(1, 0, 1, 1)   # select variables 1, 3, 4
i <- 2               # responding variable
out <- invi(sigma = sigma, c = c, i = i)
out

}
\keyword{internal}
