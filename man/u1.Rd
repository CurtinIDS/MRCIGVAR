% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{u1}
\alias{u1}
\title{Construct a shock transformation matrix for impulse response analysis}
\usage{
u1(
  B,
  sigma,
  irf = c("gen", "chol", "chol1", "gen1", "genN1", "comb", "comb1", "smat", "concerts1",
    "concerts0", "concertc"),
  comb,
  G = NA,
  smat = NA
)
}
\arguments{
\item{B}{A numeric coefficient array of a VAR object, typically with
dimensions \verb{n x n x p}, where \code{n} is the number of endogenous variables
and \code{p} is the lag order. Only the variable dimension is used here to
validate compatibility with the shock matrix.}

\item{sigma}{A numeric covariance matrix of the reduced-form residuals, with
dimension \verb{n x n}.}

\item{irf}{Character string specifying the type of shock transformation to
construct. Supported values include:
\describe{
\item{\code{"gen"}}{generalised impulse responses, scaled by residual standard
deviations.}
\item{\code{"chol"}}{standard Cholesky decomposition of \code{sigma}.}
\item{\code{"chol1"}}{column-normalised Cholesky decomposition.}
\item{\code{"gen1"}}{generalised impulse responses scaled by residual
variances.}
\item{\code{"genN1"}}{negative version of \code{"gen1"}.}
\item{\code{"comb"}}{linear combinations of shocks, scaled to unit variance.}
\item{\code{"comb1"}}{linear combinations of shocks, scaled by variance
rather than standard deviation.}
\item{\code{"smat"}}{user-supplied transformation matrix \code{smat}.}
\item{\code{"concerts1"}}{concerted shock based on \code{comb}, scaled by
variances.}
\item{\code{"concerts0"}}{concerted shock based on \code{comb}, scaled by standard
deviations.}
\item{\code{"concertc"}}{concerted conditional shock using \code{comb} and
\code{invi()}.}
\item{\code{"PTdecomp"}}{permanent-transitory decomposition based on \code{G}.}
}}

\item{comb}{Optional numeric matrix defining linear combinations or
concerted policy actions. This is required when \code{irf} is one of
\code{"comb"}, \code{"comb1"}, \code{"concerts1"}, \code{"concerts0"}, or \code{"concertc"}.}

\item{G}{Optional numeric matrix used in the permanent-transitory
decomposition when \code{irf = "PTdecomp"}. The transformation is based on the
Cholesky decomposition of \code{G \%*\% sigma \%*\% t(G)}.}

\item{smat}{Optional numeric matrix giving an explicit structural shock
decomposition. This is only used when \code{irf = "smat"}.}
}
\value{
A numeric matrix \code{smat} defining the shock transformation used for
the requested impulse response identification scheme.
}
\description{
Builds the shock transformation matrix used to generate impulse response
functions (IRFs) under different identification schemes in a VAR-type model.
Depending on the selected \code{irf} option, the function returns a matrix that
maps reduced-form innovations into orthogonalised, generalised, concerted,
or user-defined structural shocks.
}
\details{
This is an internal helper for IRF computation. It supports standard
Cholesky-based identification, generalised impulse responses, normalised
variants, concerted policy shocks, linear combinations of shocks, and an
explicitly supplied structural shock matrix.

The function returns a transformation matrix \code{smat} that determines how a
shock is defined before the impulse responses are propagated through the VAR.

Different options correspond to different identification strategies:
\itemize{
\item \strong{Cholesky-based IRFs}: \code{"chol"} and \code{"chol1"} use a recursive
ordering implied by the covariance matrix \code{sigma}.
\item \strong{Generalised IRFs}: \code{"gen"}, \code{"gen1"}, and \code{"genN1"} do not
require orthogonalisation through variable ordering in the same way as
Cholesky IRFs.
\item \strong{Combined or concerted shocks}: \code{"comb"}, \code{"comb1"},
\code{"concerts1"}, \code{"concerts0"}, and \code{"concertc"} allow shocks to be defined
as user-specified linear combinations of the reduced-form innovations.
\item \strong{User-defined structural shocks}: \code{"smat"} directly uses the
matrix supplied in \code{smat}.
\item \strong{Permanent-transitory decomposition}: \code{"PTdecomp"} uses \code{G}
to transform the covariance structure before decomposition.
}

For the \code{"comb"} and \code{"comb1"} options, the function rescales the combined
shocks using the diagonal elements of \code{t(comb) \%*\% sigma \%*\% comb}.

A dimension check is performed at the end to ensure that the number of shock
columns in \code{smat} matches the number of variables in \code{B}.
}
\examples{
\dontrun{
## Example covariance matrix
sigma = matrix(c(1.0, 0.3,
                 0.3, 2.0), 2, 2)

## Example VAR coefficient array
B = array(0, dim = c(2, 2, 1))

## Cholesky identification
u1(B, sigma, irf = "chol")

## Generalised identification
u1(B, sigma, irf = "gen")

## Combined shock affecting both variables
comb = matrix(c(1, 1,
                0, 1), 2, 2)
u1(B, sigma, irf = "comb", comb = comb)

## User-specified structural matrix
smat = diag(2)
u1(B, sigma, irf = "smat", smat = smat)
}

}
\seealso{
\code{\link{chol}}, \code{\link{diag}}
}
\keyword{internal}
