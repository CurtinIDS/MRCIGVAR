% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{w2_wmat}
\alias{w2_wmat}
\title{Convert a weight vector into a GVAR-style weighting matrix}
\usage{
w2_wmat(W)
}
\arguments{
\item{W}{A numeric vector of weights of length \code{n}.}
}
\value{
An (n, n) weighting matrix with zero diagonal and row-wise normalised
off-diagonal weights.
}
\description{
Constructs an (n x n) weighting matrix from a length-n weight vector \code{W}, where
each row \code{i} contains the normalised weights of all \emph{other} units (excluding \code{i})
and the diagonal is set to zero. In other words, for each \code{i}, the off-diagonal
entries are \code{W[-i] / sum(W[-i])}.
}
\examples{
W <- c(1, 2, 3, 4)
Wmat <- w2_wmat(W)
Wmat
rowSums(Wmat)   # should all be 1
diag(Wmat)      # should all be 0

}
\keyword{internal}
