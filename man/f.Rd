% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{f}
\alias{f}
\title{Sum of squared residuals for regime-specific cointegration vectors}
\usage{
f(x, beta, Z1, St, NSt, Y0, Z2)
}
\arguments{
\item{x}{A vector containing the parameters in the cointegration vectors (will be
reshaped to \code{dim(beta)}).}

\item{beta}{The initial value (shape template) of the cointegration vectors.}

\item{Z1}{The lagged I(1) series of the model.}

\item{St}{The indicator series of regime 1.}

\item{NSt}{The indicator series of regime 2.}

\item{Y0}{The first difference series.}

\item{Z2}{The lagged difference series.}
}
\value{
A numeric scalar: the sum of squared residuals.
}
\description{
Computes the sum of squared residuals (SSR) from a VECM-style regression where the
cointegration term is allowed to differ across two regimes. The parameter vector
\code{x} is reshaped to match \code{beta}, used to construct the cointegration index
\code{CI = Z1 \%*\% x}, and then split into regime-specific components via the indicator
series \code{St} and \code{NSt}. The SSR is computed from the regression
\code{Y0 ~ 0 + CI1 + CI2 + Z2}.
}
\examples{
set.seed(1)
T <- 50
k <- 3
r <- 1

Z1 <- matrix(rnorm(T * k), nrow = T, ncol = k)
beta <- matrix(rnorm(k * r), nrow = k, ncol = r)
x <- as.vector(beta)

St <- matrix(rbinom(T, 1, 0.5), nrow = T, ncol = 1)
NSt <- 1 - St

Y0 <- matrix(rnorm(T * r), nrow = T, ncol = r)
Z2 <- matrix(rnorm(T * 2), nrow = T, ncol = 2)

f(x = x, beta = beta, Z1 = Z1, St = St, NSt = NSt, Y0 = Y0, Z2 = Z2)

}
\keyword{internal}
