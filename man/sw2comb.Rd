% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sw2comb}
\alias{sw2comb}
\title{Build a single-row selection/weighting matrix for stacked GVAR variables}
\usage{
sw2comb(SW, n, N, K)
}
\arguments{
\item{SW}{A weighting vector of length \code{n} (will be normalised to sum to 1).}

\item{n}{Number of variables.}

\item{N}{Number of countries.}

\item{K}{Index of the selected country within each variable block (1..N).}
}
\value{
An (nN, nN) matrix with weights placed in the first column at the selected
indices.
}
\description{
Creates an (nN x nN) matrix \code{comb} with exactly one non-zero column (the first),
where \code{n} normalised weights from \code{SW} are inserted at positions corresponding to
the K-th country within each variable block. This is typically used to construct a
linear combination (selection) over a stacked (variable x country) ordering.
}
\examples{
# Example with n = 3 variables, N = 4 countries, selecting K = 2
SW <- c(2, 1, 3)
comb <- sw2comb(SW = SW, n = 3, N = 4, K = 2)
dim(comb)
comb[, 1]  # only this column has non-zeros

}
\keyword{internal}
