% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{abc_mrciva_restm}
\alias{abc_mrciva_restm}
\title{Estimate a restricted MRCIVAR model and perform a likelihood ratio test}
\usage{
abc_mrciva_restm(res = res, H = H, h = h, phi = phi, G = G, psi = psi)
}
\arguments{
\item{res}{An object containing the fitted or prepared MRCIVAR system. This
is typically the output of \code{MRCIGVARDatam} and must contain the data,
lag structure, regime information, deterministic specification, and other
quantities required by \code{mrcivar_estimatem1()}.}

\item{H}{A numeric matrix defining the linear restrictions on the
cointegration matrix \code{beta}. The restriction is written as
\code{vec(beta) = H \%*\% phi + h}.}

\item{h}{A numeric vector representing the fixed component of the linear
restrictions on \code{beta}. Non-zero elements of \code{h} can be used to impose
normalisation or other exact restrictions.}

\item{phi}{A numeric vector of free parameters in the restricted
representation of \code{beta}.}

\item{G}{A list of regime-specific restriction matrices for the adjustment
coefficients. Each element of \code{G} maps the free parameters in \code{psi[[s]]}
to \verb{vec(alpha_s')}. In the current implementation, \code{G[[1]]} and \code{G[[2]]}
correspond to the two regimes.}

\item{psi}{A list of regime-specific vectors of free parameters for the
restricted adjustment matrices. Each \code{psi[[s]]} contains the unrestricted
elements implied by \code{G[[s]]}.}
}
\value{
A list with the following components:
\describe{
\item{\code{VECMR}}{The restricted VECM estimated by least squares after imposing
the restricted cointegration relations and adjustment coefficients.}
\item{\code{BoR}}{Restricted VAR autoregressive coefficient array implied by the
restricted VECM.}
\item{\code{CoR}}{Restricted deterministic coefficient array implied by the
restricted VECM. For models without deterministic terms, this may remain
zero.}
\item{\code{sigmaR}}{Regime-specific residual covariance matrices of the
restricted model.}
\item{\code{betaR}}{Restricted estimate of the cointegration matrix \code{beta}.}
\item{\code{alphaR}}{A list containing the restricted regime-specific adjustment
matrices.}
\item{\code{LR}}{Likelihood ratio statistic comparing the restricted and
unrestricted models.}
\item{\code{p_value}}{P-value associated with the LR statistic under the
asymptotic chi-squared distribution.}
\item{\code{LH_P}}{Restricted model log-likelihood value used internally for
comparison.}
\item{\code{code}}{Convergence code returned by \code{stats::nlm()}.}
\item{\code{tst}}{Placeholder for additional test output. In the current
implementation this is returned as \code{NA}.}
}
}
\description{
Estimates a restricted Markov regime-switching cointegrated VAR (MRCIVAR)
model under user-specified linear restrictions on the adjustment matrices
(\code{alpha}) and the cointegration matrix (\code{beta}). The function fits the
restricted model by maximum likelihood, reconstructs the corresponding VECM
and VAR representation, and computes a likelihood ratio (LR) statistic for
testing the imposed restrictions against the unrestricted model.
}
\details{
The restrictions are imposed in the form
\deqn{
\mathrm{vec}(\beta) = H \phi + h
}
and, for each regime \eqn{s},
\deqn{
\mathrm{vec}(\alpha_s^\top) = G_s \psi_s.
}

This formulation allows the user to test economically meaningful hypotheses,
such as exclusion restrictions, identifying restrictions, weak exogeneity,
or theoretical long-run relations such as purchasing power parity (PPP).

The function first estimates the unrestricted MRCIVAR model using
\code{mrcivar_estimatem1()}. It then uses the unrestricted estimates as starting
values to numerically optimise the restricted likelihood via
\code{stats::nlm()}, using \code{f_constrained()} as the objective function.

After optimisation, the restricted \code{beta} and regime-specific \code{alpha}
matrices are reconstructed from the estimated free parameters:
\describe{
\item{Restricted cointegration matrix}{\code{betaR = H \%*\% phi + h}}
\item{Restricted regime 1 adjustment matrix}{\code{alpha_1 = G[[1]] \%*\% psi[[1]]}}
\item{Restricted regime 2 adjustment matrix}{\code{alpha_2 = G[[2]] \%*\% psi[[2]]}}
}

These are then used to rebuild the restricted VECM, recover the implied VAR
coefficient matrices, and compute regime-specific residual covariance
matrices. Finally, an LR statistic is formed from the restricted and
unrestricted regime-specific covariance determinants.

The function is currently written for a two-regime specification, with
separate adjustment matrices for regime 1 and regime 2.

Typical uses include:
\itemize{
\item testing weak exogeneity through zero restrictions on rows of
\code{alpha},
\item imposing identifying restrictions on \code{beta},
\item testing theory-driven long-run relationships such as PPP,
\item comparing restricted and unrestricted regime-switching VECM
specifications.
}
}
\section{Restriction examples}{

\strong{Example 1: Restrictions on \code{alpha} only (weak exogeneity).}

Suppose \code{vec(alpha)} has dimension \verb{45 x 1}, and the first variable is
assumed to be weakly exogenous, so its adjustment coefficients are fixed at
zero. Then:
\itemize{
\item \code{G} is a \verb{45 x 40} matrix selecting the unrestricted adjustment
coefficients,
\item \code{psi} is a \verb{40 x 1} vector of free parameters,
\item \code{H} is the identity matrix,
\item \code{h} is a zero vector,
\item hence \code{beta} is left unrestricted.
}

\strong{Example 2: Restrictions on \code{beta} only (PPP-type restrictions).}

Suppose \code{vec(beta)} has dimension \verb{45 x 1}, but only two elements are freely
estimated under the restriction. Then:
\itemize{
\item \code{H} is a \verb{45 x 2} matrix selecting the unrestricted elements,
\item \code{phi} is a \verb{2 x 1} vector of free parameters,
\item \code{h} contains fixed values implementing normalisation and zero
restrictions,
\item \code{G} is an identity matrix, so \code{alpha} is unrestricted.
}
}

\examples{
\dontrun{
## Example structure only - the dimensions of H, h, G, phi, and psi
## must be compatible with the fitted MRCIVAR object.

## res should be a valid MRCIVAR data/model object
## res = MRCIGVARDatam(...)

## unrestricted estimation
## res_u = mrcivar_estimatem1(res)

## identity restriction on beta (no restriction)
## H = diag(length(as.vector(res_u$tst$betaS)))
## h = rep(0, nrow(H))
## phi = rep(0, ncol(H))

## no restriction on alpha in either regime
## G = list(diag(length(as.vector(t(res_u$tst$alphaS[[1]])))),
##          diag(length(as.vector(t(res_u$tst$alphaS[[2]])))))
## psi = list(rep(0, ncol(G[[1]])), rep(0, ncol(G[[2]])))

## restricted estimation and LR test
## test_r = abc_mrciva_restm(res = res, H = H, h = h,
##                           phi = phi, G = G, psi = psi)
## test_r$LR
## test_r$p_value
}

}
\seealso{
\code{\link{mrcivar_estimatem1}},
\code{\link{f_constrained}},
\code{\link{vecm2_va_rm}},
\code{\link{rz_st2_vecm}}
}
