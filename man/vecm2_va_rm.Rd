% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{vecm2_va_rm}
\alias{vecm2_va_rm}
\title{Convert (multi-regime) VECM parameters to (multi-regime) cointegrated VAR parameters}
\usage{
vecm2_va_rm(param, beta, p = c(1, 2, 2, 2, 2), s = NA)
}
\arguments{
\item{param}{The coefficient matrix of the estimated multi-regime vector error correction model.}

\item{beta}{The estimated cointegration vectors.}

\item{p}{The lag specification. Its interpretation depends on the model class (single vs multi-regime).}

\item{s}{Types/indicator of MRCIVAR models. If \code{NA}, a single-regime model is assumed.}
}
\value{
A list with components \code{B}, \code{A}, and \code{C} corresponding to the parameters of the
(multi-regime) cointegrated VAR.
}
\description{
Converts an estimated vector error correction model (VECM) parameter matrix into
the corresponding cointegrated VAR parameter arrays in levels. The function supports
both single-regime and multi-regime specifications, including CIVAR/CIGVAR and their
multi-regime counterparts (MRCIVAR/MRCIGVAR), depending on the lag specification \code{p}
and the regime indicator \code{s}.
}
\details{
The output contains:
\itemize{
\item \code{B}: domestic VAR coefficient array (in levels),
\item \code{A}: foreign/conditioning VAR coefficient array (in levels) when applicable, and
\item \code{C}: deterministic-component parameters when present.
}
}
\examples{
# Minimal illustrative example (fabricated shapes; relies on cib3_b() and cia2_a())
set.seed(1)
m <- 3
p <- c(1, 2, 0, 2, 2)   # single-regime CIVAR example uses p[1], p[2], p[3]

# param is used transposed as VECB; build as (k x m)
k <- p[1] + (p[2] + p[3]) * m + 1
param <- matrix(rnorm(k * m), nrow = k, ncol = m)

# beta must conform for AB = VECB[, 1:p[1]] \%*\% t(beta)
beta <- matrix(rnorm(m * p[1]), nrow = m, ncol = p[1])

out <- vecm2_va_rm(param = param, beta = beta, p = p, s = NA)
str(out)

}
\keyword{internal}
